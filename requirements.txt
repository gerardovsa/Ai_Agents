# Web Framework
flask==3.0.0
flask-cors==4.0.0
flask-socketio==5.5.1
python-socketio==5.12.0
websocket-client==1.6.4
waitress==2.1.2
gunicorn==21.2.0

# Async Workers (for Gunicorn)
gevent>=23.9.0
gevent-websocket>=0.10.1

# AI Models
anthropic>=0.40.0
openai==1.35.0
requests==2.31.0

# Voice Transcription (Local Whisper Model)
openai-whisper>=20231117
torch>=2.0.0
torchaudio>=2.0.0

# Data Processing
pandas==2.1.4
numpy>=1.26.4

# Environment
python-dotenv==1.0.0

# Google APIs (loosened constraints to resolve dependency conflicts)
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-api-python-client>=2.100.0
google-cloud-run>=0.10.0
google-cloud-logging>=3.8.0
google-cloud-monitoring>=2.16.0

# Database
psycopg2-binary==2.9.9
supabase>=2.0.0,<3.0  # Pin to avoid gotrue backtracking
sqlalchemy==2.0.23
pyodbc>=5.0.0
pymssql>=2.2.0

# Vector Database
pinecone>=3.0.0
qdrant-client>=1.7.0,<2.0  # Pin to prevent excessive version backtracking

# Scheduler
apscheduler>=3.10.0

# Document Processing & Text Extraction (Markdown Support)
python-docx==1.1.0      # DOCX extraction with formatting
openpyxl>=3.1.0         # XLSX/XLS extraction with tables
python-pptx>=1.0.0      # PPTX extraction with slides
pypdf>=3.17.0
PyPDF2>=3.0.0
reportlab>=4.0.0        # PDF generation from documents
Pillow>=10.0.0          # Image conversion and manipulation
pytesseract>=0.3.10

# Document Conversion (PDF/Image Generation - NEW Dec 2025)
# docx2pdf>=0.1.8       # DOCX to PDF (Windows only - optional)
pdf2image>=1.16.0       # PDF to images (requires poppler)
img2pdf>=0.5.0          # Images to PDF

# Text/HTML Processing
html2text>=2020.1.16
beautifulsoup4>=4.12.0  # HTML extraction
lxml>=4.9.0             # XML/HTML parsing

# Geolocation (optional) - removed maxminddb-geolite2 due to slow download
# geoip2>=4.7.0
# maxminddb-geolite2>=2018.701

# Token counting
tiktoken>=0.5.0

# Utilities
pytz==2023.3
python-dateutil==2.8.2

# Authentication & Security (NEW)
bcrypt==4.0.1
PyJWT==2.8.0

# Engineering & CAD Libraries (NEW Dec 10, 2025)
# Note: Pin dependencies first to avoid backtracking during Docker builds
matplotlib>=3.9.2,<3.11   # Required by sectionproperties>=3.9.0, pinned for Python 3.11 compatibility
PyNiteFEA>=1.6.2          # Structural engineering calculations (beam deflection, stress analysis)
sectionproperties>=3.3.0,<3.9.0  # Cross-section properties (3.9.0 requires cytriangle>=2.0 which needs numpy>=2.1)
forallpeople>=2.7.1       # Unit conversion and dimensional analysis
handcalcs>=1.10.0         # Beautiful engineering calculation documentation
cadquery>=2.6.1           # Parametric 3D CAD model generation (STEP/STL export)
ezdxf>=1.4.3              # 2D CAD (DXF/DWG) file generation and editing
